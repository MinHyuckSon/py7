#변수 - 숫자(정수,실수),문자열,불(True,False)
# 리스트, 튜플, 셋
# 리스트 - 베열 : 데이터를 여러개 저장하는 공간
#변수는 1개의 값만 저장

fruit =["사과","배","복숭아","딸기","포도"]
print(fruit[0])
numbers = [1,2,3,4,5]
print(numbers)
print(numbers[0])
#파이선의 리스트는 다양한 형태의 자료형이 들어올 수 있다.
total = ["사과",1,2,True,1.25]
print(totoal)
stuscore = ['홍길동',100,100]
stuscore.insert(1,5) #1번째 방에 값 5를 추가

# print(stuscore)
# stuscore.append(100) #append는 배열의 맨뒤에 추가함
# print(stuscore)

### 리스트 데이터 삭제 = pop,remove,del
# stuscore.pop()      #pop은 제일 뒤의 값 삭제
# print(stuscore)

# stuscore.remove("홍길동")
# print(stuscore)       #remove는 해당 값 삭제

# stuscore.remove(100)
# print(stuscore)

#del 해당 배열안에 번지수 삭제
