#로또 번호 맞추기 프로그램
#1. 변수선언, 로또번호 생성
import random

iLotto = random.sample(range(1,46),6)  #샘플 함수 안에 제한되는 숫자를 넣는다. 1에서45까지 랜덤이어도 상관없이
                                       #45개의 숫자를 받은 후 샘플함수로 중복되지 않게 6개를 걸러낸다. 
iNumbers = []
iCount = 0
#2. 숫자입력

for i in range(6):
    iRange = 0
    iRange = input("1~45까지의 번호를 입력하세요")
    if 0 < iRange < 46:
        iNumbers.append()
    else:
        print("숫자범위가 아닙니다.")

#3. 당첨번호 확인

#4. 결과화면 출력

#5. 당첨금 출력